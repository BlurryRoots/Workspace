# load config
plugin_dir=$(dirname $(readlink -f "$BASH_SOURCE[0]"))
source "$plugin_dir/config"

# make sure path is existing
mkdir -p "$subl_config_path"

# copy
cp -i "$(get_subl_keymap_name)" "$(get_subl_user_data_path)/$(get_subl_keymap_name)"